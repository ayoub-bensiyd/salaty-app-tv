# 🔨 دليل البناء السريع

## المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

1. ✅ **Android Studio** (Hedgehog أو أحدث)
2. ✅ **JDK 8+**
3. ✅ **Android SDK 24+**

## خطوات البناء

### 1️⃣ فتح المشروع

```bash
# افتح Android Studio
# اختر File → Open
# حدد مجلد tv-app
```

### 2️⃣ إضافة ملفات الصوت (⚠️ إلزامي)

**يجب** إضافة الملفات التالية في `app/src/main/res/raw/`:

- `adhan_makkah.mp3` - صوت الأذان
- `iqama.mp3` - صوت الإقامة

راجع ملف `AUDIO_FILES_GUIDE.md` للتفاصيل.

### 3️⃣ مزامنة Gradle

```bash
# في Android Studio:
# File → Sync Project with Gradle Files
```

انتظر حتى تكتمل المزامنة (قد يستغرق بضع دقائق في المرة الأولى).

### 4️⃣ بناء APK

#### من Android Studio:

1. اختر **Build → Build Bundle(s) / APK(s) → Build APK(s)**
2. انتظر حتى يظهر إشعار "Build Successful"
3. اضغط على **locate** لفتح مجلد APK

#### من سطر الأوامر:

```bash
# على Linux/Mac:
./gradlew assembleDebug

# على Windows:
gradlew.bat assembleDebug
```

### 5️⃣ موقع ملف APK

بعد البناء الناجح، ستجد APK في:

```
app/build/outputs/apk/debug/app-debug.apk
```

## 📦 التثبيت على Android Box

### الطريقة 1: عبر USB

1. انسخ `app-debug.apk` إلى محرك USB
2. أدخل USB في Android Box
3. افتح مدير الملفات
4. انتقل إلى USB وابحث عن APK
5. اضغط على الملف للتثبيت

### الطريقة 2: عبر ADB

```bash
# قم بتوصيل Android Box بالكمبيوتر
# فعّل USB Debugging على Android Box

adb install app/build/outputs/apk/debug/app-debug.apk
```

## ⚙️ الإعدادات المطلوبة على Android Box

### تفعيل مصادر غير معروفة:

1. افتح **الإعدادات**
2. اذهب إلى **الأمان** أو **Security**
3. فعّل **مصادر غير معروفة** أو **Unknown Sources**

### تفعيل USB Debugging (اختياري):

1. افتح **الإعدادات**
2. اذهب إلى **حول الجهاز** أو **About**
3. اضغط على **رقم الإصدار** 7 مرات لتفعيل وضع المطور
4. ارجع إلى الإعدادات → **خيارات المطور**
5. فعّل **USB Debugging**

## 🧪 الاختبار

بعد التثبيت:

1. ✅ افتح التطبيق
2. ✅ تحقق من ظهور الساعة والتاريخ
3. ✅ تحقق من أوقات الصلاة
4. ✅ اضغط مع الاستمرار على OK لمدة 3 ثوان
5. ✅ أدخل PIN: `1234`
6. ✅ اختبر الإعدادات

## 🐛 حل المشاكل الشائعة

### خطأ: "Gradle sync failed"

**الحل**:
```bash
# احذف مجلدات الكاش
rm -rf .gradle
rm -rf .idea

# أعد فتح المشروع في Android Studio
```

### خطأ: "SDK not found"

**الحل**:
1. افتح **File → Project Structure**
2. تأكد من تحديد SDK الصحيح
3. اضغط **Apply**

### خطأ: "Cannot find adhan_makkah.mp3"

**الحل**:
- تأكد من إضافة ملفات الصوت في `app/src/main/res/raw/`
- راجع `AUDIO_FILES_GUIDE.md`

### خطأ: "Installation failed"

**الحل**:
- تأكد من تفعيل "مصادر غير معروفة"
- تحقق من مساحة التخزين المتاحة
- أعد تشغيل Android Box

## 📊 معلومات البناء

- **Package Name**: `com.prayertv.oujda`
- **Min SDK**: 24 (Android 7.0)
- **Target SDK**: 34 (Android 14)
- **Version**: 1.0

## 🚀 بناء نسخة الإنتاج (Release)

لبناء نسخة موقعة للإنتاج:

### 1. إنشاء Keystore:

```bash
keytool -genkey -v -keystore prayer-tv.keystore -alias prayertv -keyalg RSA -keysize 2048 -validity 10000
```

### 2. إضافة معلومات التوقيع في `app/build.gradle.kts`:

```kotlin
android {
    signingConfigs {
        create("release") {
            storeFile = file("../prayer-tv.keystore")
            storePassword = "your_password"
            keyAlias = "prayertv"
            keyPassword = "your_password"
        }
    }
    
    buildTypes {
        release {
            signingConfig = signingConfigs.getByName("release")
        }
    }
}
```

### 3. بناء Release APK:

```bash
./gradlew assembleRelease
```

## 📝 ملاحظات

- أول بناء قد يستغرق وقتاً أطول (تنزيل التبعيات)
- تأكد من اتصال إنترنت جيد للبناء الأول
- احتفظ بنسخة احتياطية من keystore للإنتاج

## 🆘 الدعم

إذا واجهت مشاكل:

1. راجع ملف `README.md`
2. تحقق من سجل الأخطاء في Android Studio (Logcat)
3. افتح Issue على GitHub

---

**نصيحة**: استخدم **Build → Clean Project** ثم **Build → Rebuild Project** إذا واجهت مشاكل غريبة!
